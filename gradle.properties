org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
kotlin.code.style=official
# AGP 8.7.2 was only tested up to compileSdk 35; 36 is intentional (matches the
# installed platform and targetSdk) and builds fine, just untested by AGP's own CI.
android.suppressUnsupportedCompileSdk=36
